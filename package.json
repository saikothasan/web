{
  "name": "cloudai",
  "version": "1.0.0",
  "description": "A Cloudflare Worker using Puppeteer and AI for browser rendering and data extraction.",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250709.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.3",
    "zod": "^4.0.0",
    "zod-to-json-schema": "^3.24.6"
  }
}
